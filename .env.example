# Dash Core RPC settings
DASH_RPC_USER=dashrpc
DASH_RPC_PASSWORD=your_secure_password_here
DASH_RPC_HOST=localhost
DASH_RPC_PORT=19998

# Rate limiting
RATE_LIMIT_PER_HOUR=3

# Credit amount in duffs (1 DASH = 100,000,000 duffs)
CREDIT_AMOUNT=100000000

# InstantLock timeout in seconds
ISLOCK_TIMEOUT=30

# CAP captcha settings (optional)
CAP_API_ENDPOINT=http://localhost:3000
CAP_SITE_KEY=
CAP_SECRET=

# Hard CAP for rate limit bypass (higher difficulty, optional)
CAP_HARD_SITE_KEY=
CAP_HARD_SECRET=

# Trusted server-to-server API (JSON map of key IDs to SHA-256 key hashes)
# Example: {"platform-team":"0123456789abcdef..."}
FAUCET_API_KEY_HASHES=
FAUCET_API_KEY_DAILY_LIMIT=100

# Promo codes as JSON (optional, merged with promo_codes.json; env wins on conflict)
# PROMO_CODES={"TESTCODE": {"amount": 5.0}}
